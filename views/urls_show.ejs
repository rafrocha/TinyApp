<!DOCTYPE html>
<html lang="en">

<head>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
  <div class="container-fluid">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <title>TinyApp</title>
</head>

<center>
  <div style="margin: auto;">
    <h1> TINY APP </h1>
<h5> A URL shortener </h5>
    <% include partials/_header %>
      <p style="margin: 30px 0px 30px 0px">
        <a href="<%= urls[shortURL].longURL %>">
          <%= shortURL %>
        </a> -
        <%= urls[shortURL].longURL %> <br>
        Times visited: <%= urls[shortURL].count %>
      </p>
      <form action="/urls/<%= shortURL %>/?_method=PUT" method="POST" style="width: 300px;">
        <label for="newLongURL">Assign new URL to
          <%= shortURL %>:</label>
        <input class="form-control" id="longURL" type="text" name="newLongURL" placeholder="<%= urls[shortURL].longURL %>">
        <button type="submit" class="btn btn-primary" style="margin: 10px 0px 10px 0px;">Edit</button>
      </form>
      Send to a Friend! <button onclick="copyToClipboard('#p1')"><span class="glyphicon glyphicon-send"></span></button><br>
      <p hidden id="p1"> localhost:8080/u/<%= shortURL %></p>
      <% if (user_id) { %>
        <p><a href="/urls/new"> Short another one! </a></p>
        <% } else {%>
          <p> Log in/Register to shorten a URL</p>
          <% } %>
            <p> <a href="/urls"> Back to URLs </a> </p>
  </div>
</center>
</div>


<script>
  function copyToClipboard(element) {
  var $temp = $("<input>");
  $("body").append($temp);
  $temp.val($(element).text()).select();
  document.execCommand("copy");
  $temp.remove();
}
</script>


</html>